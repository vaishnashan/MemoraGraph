1. To run ingestion
 uvicorn application.ingestion.main:app --reload 
 http://localhost:8000/docs


2.To run mcp
  python -m application.mcp.server

  npx @modelcontextprotocol/inspector python -m application.mcp.server

3.docker
  docker compose build --no-cache - runn it once 

  docker compose up memoragraph-ingestion -  Runs in the foreground so you see live logs. Wait for "Application startup complete.
  docker compose run --rm memoragraph-mcp -  run The mcp service has profiles: 
  
http://localhost:8000/docs
MCP is something a *program launches and talks to*, never something you open in a tab.