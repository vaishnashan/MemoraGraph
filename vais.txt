python -m application.graph2.test_falkordb    

python -m application.graph2.test_entity_extraction_gemini

python -m application.graph2.test_entity_extraction_groq  


python -m application.parsing3.test_docling

python -m application.embeddings.testembed   
uvicorn application.main:app --reload

python -m application.mcp_server8.server
npx @modelcontextprotocol/inspector python -m application.mcp_server8.server

docker compose up memoragraph-api
docker compose run --rm memoragraph-mcp



docker compose build --no-cache
docker compose up -d memoragraph-api
http://localhost:8000/docs
docker compose run --rm memoragraph-mcp

# Build both images (one build, since both services use the same Dockerfile)
docker compose build

# Terminal 1 — start the API
docker compose up memoragraph-api

# Terminal 2 — verify it's healthy
curl http://localhost:8000/health

# Terminal 3 — run the MCP server interactively
docker compose run --rm memoragraph-mcp


# 4. Now run them
docker compose up memoragraph-api
docker compose run --rm memoragraph-mcp
